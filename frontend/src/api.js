// import axios from 'axios';

// const API_URL = 'http://127.0.0.1:5000'; // Update if hosted

// const predictSpam = async (message) => {
//     try {
//         const response = await axios.post(`${API_URL}/predict`, { message });
//         return response.data;
//     } catch (error) {
//         console.error("Error predicting spam:", error);
//         return { error: "Failed to connect to backend" };
//     }
// };

// export default predictSpam;  // Ensure it's a default export
